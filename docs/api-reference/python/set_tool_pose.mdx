---
title: set_tool_pose
---

Set the tool pose of the robot arm.

## Parameters

- `pose`: List of floats containing the tool pose. x, y, z, roll, pitch, yaw.

## Example Usage

```python
await client.set_tool_pose([0.1, 0.2, 0.3, 0, 0, 0])
``` 